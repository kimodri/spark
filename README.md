# Spark Notes
Note: 
- `df.rdd.getNumPartitions()`
- `app_name = spark.conf.get("spark.app.name")`
This repository will contain my spark notes and a little bit of Databricks specifically by how they connect